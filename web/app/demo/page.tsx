import { DemoJourney } from '@/components/demo/DemoJourney';

export const metadata = {
  title: 'Explore Proofmark',
  description: 'Try sample screening and explore credential access requirements on Creditcoin Testnet.',
};

export default function DemoPage() {
  return <DemoJourney />;
}
