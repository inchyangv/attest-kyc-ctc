# 비밀값 — 절대 커밋 금지
.env
.env.*
!.env.example
*.key
*.pem

# 서드파티 클론 (튜토리얼 참고용, 우리 저작물 아님)
reference/

# 빌드 산출물
node_modules/
dist/
out/
cache/
broadcast/
artifacts/
.DS_Store

# foundry 의존성 (forge install)
lib/

# 워커 런타임 상태
state/

# 제재 명단 원본 (57MB) — aml/fetch-lists.sh 로 받는다
data/raw/
state/
